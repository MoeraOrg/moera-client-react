import React from 'react';
import { Field } from 'formik';
import { Form } from 'react-bootstrap';

const FieldControl = ({name, ...props}) => (
    <Field name={name} render={
        ({field, form}) => (
            <Form.Control {...field} {...props} />
        )
    }/>
);

export default FieldControl;