export const PREFIX = "client.mercy.";

export const META = [
    {
        name: "posting.time.relative",
        type: "bool",
        defaultValue: "false",
        title: "Show relative time in postings",
        modifiers: {}
    }
];

export function buildMetaMap() {
    let metadata = new Map();
    META.forEach(meta => metadata.set(meta.name, meta));
    return metadata;
}
