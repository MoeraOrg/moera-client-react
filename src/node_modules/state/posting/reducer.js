import { GO_TO_PAGE } from "state/navigation/actions";
import { PAGE_POSTING } from "state/navigation/pages";

const initialState = {
    id: null
};

export default (state = initialState, action) => {
    switch (action.type) {
        case GO_TO_PAGE:
            if (action.payload.page === PAGE_POSTING) {
                return {
                    ...state,
                    id: action.payload.details.id
                }
            }
            return state;

        default:
            return state;
    }
}
