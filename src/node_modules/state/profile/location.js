import { isProfileEditing } from "state/profile/selectors";

export default (state, info) => {
    info = info.sub("profile");
    return isProfileEditing(state) ? info.withParameter("edit", "true") : info;
}
