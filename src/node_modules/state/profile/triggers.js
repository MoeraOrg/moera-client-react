import { trigger } from "state/trigger";
import { CONNECTED_TO_HOME, DISCONNECT_FROM_HOME } from "state/home/actions";
import { profileLoad } from "state/profile/actions";

export default [
    trigger([CONNECTED_TO_HOME, DISCONNECT_FROM_HOME], true, profileLoad)
];
