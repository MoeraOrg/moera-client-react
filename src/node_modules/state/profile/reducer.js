import {
    PROFILE_EDIT,
    PROFILE_EDIT_CANCEL,
    PROFILE_LOAD,
    PROFILE_LOAD_FAILED,
    PROFILE_SET, PROFILE_UPDATE, PROFILE_UPDATE_FAILED,
    PROFILE_UPDATE_SUCCEEDED
} from "state/profile/actions";

const initialState = {
    loading: false,
    registeredName: null,
    registeredNameGeneration: null,
    fullName: null,
    gender: null,
    operations: {
        edit: []
    },
    editing: false,
    updating: false
};

export default (state = initialState, action) => {
    switch (action.type) {
        case PROFILE_LOAD:
            return {
                ...state,
                loading: true
            };

        case PROFILE_SET:
            return {
                ...state,
                ...action.payload.profile,
                loading: false
            };

        case PROFILE_LOAD_FAILED:
            return {
                ...state,
                loading: false
            };

        case PROFILE_EDIT:
            return {
                ...state,
                editing: true,
            };

        case PROFILE_UPDATE:
            return {
                ...state,
                updating: true
            };

        case PROFILE_EDIT_CANCEL:
        case PROFILE_UPDATE_SUCCEEDED:
            return {
                ...state,
                editing: false,
                updating: false
            };

        case PROFILE_UPDATE_FAILED:
            return {
                ...state,
                updating: false
            };

        default:
            return state;
    }
}
