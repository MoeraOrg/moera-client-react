export const OWNER_LOAD = "OWNER_LOAD";
export const ownerLoad = () => ({
    type: OWNER_LOAD
});

export const OWNER_SET = "OWNER_SET";
export const ownerSet = (name, generation) => ({
    type: OWNER_SET,
    payload: {name, generation}
});

export const OWNER_VERIFY = "OWNER_VERIFY";
export const ownerVerify = () => ({
    type: OWNER_VERIFY
});

export const OWNER_VERIFIED = "OWNER_VERIFIED";
export const ownerVerified = (name, generation, latest, correct, deadline) => ({
    type: OWNER_VERIFIED,
    payload: {name, generation, latest, correct, deadline}
});
