import { trigger } from "state/trigger";
import { TIMELINE_SCROLLED } from "state/timeline/actions";
import { scrollLocation } from "state/navigation/actions";

export default [
    trigger(TIMELINE_SCROLLED, true, scrollLocation)
];
