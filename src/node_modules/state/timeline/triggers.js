import { conj, trigger } from "state/trigger";
import { GO_TO_PAGE } from "state/navigation/actions";
import { isAtTimelinePage } from "state/navigation/selectors";
import { isTimelineSliceToBeLoaded } from "state/timeline/selectors";
import { timelinePastSliceLoad } from "state/timeline/actions";

export default [
    trigger(GO_TO_PAGE, conj(isAtTimelinePage, isTimelineSliceToBeLoaded), timelinePastSliceLoad),
];
