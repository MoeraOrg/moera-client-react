import { PAGE_INDEX, PAGE_PROFILE } from "state/navigation/pages";

export function isAtIndexPage(state) {
    return state.navigation.page === PAGE_INDEX;
}

export function isAtProfilePage(state) {
    return state.navigation.page === PAGE_PROFILE;
}
