import { isAtProfilePage } from "state/navigation/selectors";
import profileLocation from "state/profile/location";

export default (state, info) => {
    if (isAtProfilePage(state)) {
        return profileLocation(state, info);
    }
    return info;
}
