import React from 'react';
import { connect } from 'react-redux';

import ConnectionStatus from "ui/logobar/connectionstatus/ConnectionStatus";
import { goToTimeline } from "state/navigation/actions";
import "./LogoBar.css";

const LogoBar = ({goToTimeline}) => (
    <div id="logo-bar">
        <div id="logo-bar-left">
            <a href="/moera" onClick={event => {
                goToTimeline();
                event.preventDefault();
            }}>
                <img id="logo-image" src="/pics/logo-w-64.png" alt="Moera" />
            </a>
        </div>
        <div id="logo-bar-right">
            <ConnectionStatus />
        </div>
    </div>
);

export default connect(
    null,
    { goToTimeline }
)(LogoBar);
