import React from 'react';
import { connect } from 'react-redux';

import { Loading } from "ui/control";

import "./SettingsTabs.css";

const SettingsTabs = ({loadingNodeValues}) => (
    <ul className="nav nav-tabs settings-tabs">
        <li className="nav-item">
            <span className="nav-link active">Node <Loading active={loadingNodeValues} /></span>
        </li>
        <li className="nav-item">
            <a className="nav-link" href="#">Client</a>
        </li>
    </ul>
);

export default connect(
    state => ({
        loadingNodeValues: state.settings.node.loadingValues
    })
)(SettingsTabs);
