import React from 'react';
import { connect } from 'react-redux';
import Events from "ui/events/Events";

const NodeEvents = ({homeEvents}) => (
    <Events location={homeEvents}/>
);

export default connect(
    state => ({
        homeEvents: state.home.root.events
    })
)(NodeEvents);
