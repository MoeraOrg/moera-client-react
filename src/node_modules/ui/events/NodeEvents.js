import React from 'react';
import { connect } from 'react-redux';
import Events from "ui/events/Events";

const NodeEvents = ({nodeEvents}) => (
    <Events location={nodeEvents}/>
);

export default connect(
    state => ({
        nodeEvents: state.node.root.events
    })
)(NodeEvents);
