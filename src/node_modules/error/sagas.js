import { put, delay } from 'redux-saga/effects';

import { errorDismiss, errorShow } from "error/actions";

export function* errorSaga(action) {
    yield put(errorShow(action.payload.e.message));
    yield delay(7000);
    yield put(errorDismiss());
}
