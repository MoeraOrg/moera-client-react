import {
    CANCEL_CONNECT_DIALOG,
    OPEN_CONNECT_DIALOG
} from "logobar/connectionstatus/connectdialog/actions";
import { CONNECT_TO_HOME } from "logobar/connectionstatus/actions";

const initialState = {
    show: false,
    location: "",
    login: "admin"
};

export default (state = initialState, action) => {
    switch (action.type) {
        case OPEN_CONNECT_DIALOG:
            return {
                ...state,
                show: true
            };

        case CANCEL_CONNECT_DIALOG:
            return {
                ...state,
                show: false
            };

        case CONNECT_TO_HOME:
            return {
                ...state,
                location: action.payload.location,
                login: action.payload.login,
                show: false
            };

        default:
            return state;
    }
}
