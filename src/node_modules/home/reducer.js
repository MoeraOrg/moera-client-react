const initialState = {
    home: {
        root: {
            location: null,
            page: null,
            api: null
        }
    }
};

export default (state = initialState, action) => {
    return state;
}
