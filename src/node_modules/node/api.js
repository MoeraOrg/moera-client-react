import schema from 'schm';

export const WhoAmI = schema({
    registeredName: {
        type: String,
        required: true
    },
    registeredNameGeneration: {
        type: Number,
        required: true
    }
});
