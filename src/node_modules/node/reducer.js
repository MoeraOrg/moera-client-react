export default (state, action) => {
    if (state === undefined) {
        const location = window.location.protocol + "//" + window.location.host;
        const page = location + "/moera";
        const api = page + "/api";
        return {root: {location, page, api}};
    } else {
        return state;
    }
}
