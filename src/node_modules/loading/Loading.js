import React from 'react';
import ReactLoading from 'react-loading';

import "loading/Loading.css";

export const Loading = ({active}) => (
    active && <ReactLoading className="loading" type="bubbles" color="black" width={32} height={32} />
);
