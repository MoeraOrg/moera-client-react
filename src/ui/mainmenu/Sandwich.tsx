import { Icon, msMenu } from "ui/material-symbols";
import "./Sandwich.css";

const Sandwich = () => (
    <button id="sandwich">
        <Icon icon={msMenu} size={24}/>
    </button>
);

export default Sandwich;
