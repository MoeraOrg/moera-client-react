import React from 'react';

import ComposeIconButton from "ui/compose/ComposeIconButton";

const ComposeCommentsButton = () =>
    <ComposeIconButton icon="comment" name="commentVisible" tooltip="Comments" changed={false}/>;

export default ComposeCommentsButton;
